# KRaft, брокер {{ item.id }}
process.roles=broker,controller
node.id={{ item.id }}
controller.quorum.voters={{ kafka_kraft_quorum_voters }}
controller.listener.names=CONTROLLER

listeners=PLAINTEXT://0.0.0.0:{{ item.broker_port }},CONTROLLER://{{ kafka_controller_bind_address }}:{{ item.controller_port }}
inter.broker.listener.name=PLAINTEXT
advertised.listeners=PLAINTEXT://{{ kafka_advertised_host }}:{{ item.broker_port }}
listener.security.protocol.map=CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT

log.dirs={{ item.log_dir }}

num.network.threads=3
num.io.threads=8
socket.send.buffer.bytes=102400
socket.receive.buffer.bytes=102400
socket.request.max.bytes=104857600
num.partitions=1
num.recovery.threads.per.data.dir=1
offsets.topic.replication.factor=3
transaction.state.log.replication.factor=3
transaction.state.log.min.isr=2

log.retention.hours=168
